# Hands On
```
cd ../shipping/
cp ../catalog/.gitlab-ci.yml ./
cp ../catelog/src/main/resources/bootstrap.yml src/main/resources/
```

```
git init
git status
rm -f .mvn/
git add pom.xml .gitignore src/
git commit -m"starting project"
```
